<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home</title>
</head>
<body>

<h1>The Green Byte Bistro</h1>
     <p> name:<%= RESTAURANT.name %></p>
     <p> adress:<%= RESTAURANT.address %></p> 
     <p> phone:<%= RESTAURANT.phone %></p>

    <% RESTAURANT.menu.forEach(function(oneItem){   %>
        <hr>
        <p><%= oneItem.name %></p>
        <p>BD <%= oneItem.price %></p>
        <p><%= oneItem.rating %></p>
        <hr>
    
    <% }) %>
</body>
</html>